# job-eligibility-checker
This project is a basic rule based screening system built in Python. It takes user input, processes it using conditional logic, and returns a classification based on defined criteria. It simulates a simple hiring filter using structured rules and user data.
